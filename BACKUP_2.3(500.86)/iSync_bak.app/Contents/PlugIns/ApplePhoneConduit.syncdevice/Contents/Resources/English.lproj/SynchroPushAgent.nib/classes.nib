{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancel = id; 
                displayAgentNotFoundMessage = id; 
                displayAgentTooOldMessage = id; 
                displayPreparePushMessage = id; 
                goToAuthorizationInfoPane = id; 
                pushAgent = id; 
                pushAgentDone = id; 
                showHelpForAgentNotFound = id; 
                tryAgainPushingAgent = id; 
            }; 
            CLASS = PhCPushAgentController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                agentNotFoundHelpButton = id; 
                infoTabView = NSTabView; 
                progressIndicator = NSProgressIndicator; 
                pushingTextHeaderField = NSTextField; 
                statusField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}